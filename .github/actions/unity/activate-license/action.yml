# ┌──────────────────────────────────────────────────────────────────┐
# │  Author: Ivan Murzak (https://github.com/IvanMurzak)             │
# │  Repository: GitHub (https://github.com/IvanMurzak/Unity-MCP)    │
# │  Copyright (c) 2025 Ivan Murzak                                  │
# │  Licensed under the Apache License, Version 2.0.                 │
# │  See the LICENSE file in the project root for more information.  │
# └──────────────────────────────────────────────────────────────────┘

name: "Unity Activate License"
description: "Use a provided Unity ULF license or activate one via unity-license-activate and output the ULF content"

inputs:
  unityVersion:
    description: "Unity version to activate (e.g. 2022.3.62f3)"
    required: true
  unity-license:
    description: "Unity license file content (ULF XML). If provided, skips web activation."
    required: false
    default: ""
  unity-email:
    description: "Unity account email"
    required: false
    default: ""
  unity-password:
    description: "Unity account password"
    required: false
    default: ""

outputs:
  license:
    description: "Unity license file content (ULF XML, base64-encoded)"
    value: ${{ steps.resolve.outputs.content }}

runs:
  using: "composite"
  steps:
    - name: Setup Node.js
      if: inputs.unity-license == ''
      uses: actions/setup-node@v6
      with:
        node-version: "20"

    - name: Install unity-license-activate
      if: inputs.unity-license == ''
      run: npm install --global unity-license-activate@0.3.9
      shell: bash

    - name: Generate Unity activation file (.alf)
      id: alf
      if: inputs.unity-license == ''
      env:
        UNITY_VERSION: ${{ inputs.unityVersion }}
      run: |
        mkdir -p .unity-license

        docker run --rm \
          -v "$(pwd)/.unity-license:/output" \
          -w /output \
          unityci/editor:ubuntu-${UNITY_VERSION}-base-3 \
          unity-editor -batchmode -createManualActivationFile -logFile /dev/stdout || true

        ALF_FILE=$(find .unity-license -name "*.alf" | head -1)
        if [ -z "$ALF_FILE" ]; then
          echo "Failed to generate .alf file"
          exit 1
        fi
        echo "Generated ALF file: $ALF_FILE"
        echo "alf_path=${ALF_FILE}" >> "$GITHUB_OUTPUT"
      shell: bash

    - name: Use provided Unity license
      id: inline
      if: inputs.unity-license != ''
      env:
        UNITY_LICENSE_INPUT: ${{ inputs.unity-license }}
      run: |
        encoded=$(printf '%s' "$UNITY_LICENSE_INPUT" | base64 -w 0)
        echo "content=$encoded" >> "$GITHUB_OUTPUT"
      shell: bash

    - name: Activate Unity license
      id: ulf
      if: inputs.unity-license == ''
      env:
        UNITY_EMAIL: ${{ inputs.unity-email }}
        UNITY_PASSWORD: ${{ inputs.unity-password }}
      run: |
        if [ -z "$UNITY_EMAIL" ] || [ -z "$UNITY_PASSWORD" ]; then
          echo "UNITY_EMAIL and UNITY_PASSWORD are required for license activation."
          exit 1
        fi

        echo "::add-mask::$UNITY_EMAIL"
        echo "::add-mask::$UNITY_PASSWORD"

        MAX_RETRIES=5
        for attempt in $(seq 1 $MAX_RETRIES); do
          echo "=== License activation attempt $attempt of $MAX_RETRIES ==="
          if unity-license-activate "$UNITY_EMAIL" "$UNITY_PASSWORD" "${{ steps.alf.outputs.alf_path }}"; then
            echo "unity-license-activate succeeded on attempt $attempt"
            break
          fi
          if [ "$attempt" -eq "$MAX_RETRIES" ]; then
            echo "All $MAX_RETRIES license activation attempts failed"
            exit 1
          fi
          echo "Attempt $attempt failed, retrying in 10s..."
          sleep 10
        done

        ULF_FILE=$(find . -maxdepth 2 -name "*.ulf" | head -1)
        if [ -z "$ULF_FILE" ]; then
          echo "Failed to obtain .ulf license file"
          exit 1
        fi
        echo "Found ULF file: $ULF_FILE"

        encoded=$(base64 -w 0 "$ULF_FILE")
        echo "content=$encoded" >> "$GITHUB_OUTPUT"
      shell: bash

    - name: Resolve Unity license output
      id: resolve
      env:
        INLINE_LICENSE_B64: ${{ steps.inline.outputs.content }}
        ACTIVATED_LICENSE_B64: ${{ steps.ulf.outputs.content }}
      run: |
        if [ -n "$INLINE_LICENSE_B64" ]; then
          echo "content=$INLINE_LICENSE_B64" >> "$GITHUB_OUTPUT"
          exit 0
        fi

        if [ -n "$ACTIVATED_LICENSE_B64" ]; then
          echo "content=$ACTIVATED_LICENSE_B64" >> "$GITHUB_OUTPUT"
          exit 0
        fi

        echo "No Unity license content was produced."
        exit 1
      shell: bash

    - name: Upload error screenshot
      if: failure()
      uses: actions/upload-artifact@v6
      with:
        name: unity-license-error-${{ inputs.unityVersion }}
        path: error.png
        if-no-files-found: ignore
